# Supabase — copy to .env and fill in from your project (Settings → API).
# EXPO_PUBLIC_ vars are inlined into the client bundle; use only the anon key
# (publishable by design), never the service_role key.
EXPO_PUBLIC_SUPABASE_URL=https://esewfdrusxdoopieyhxo.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_YOUR-PUBLISHABLE-KEY
